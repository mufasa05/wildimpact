{
  "version": 2,
  "public": true,
  "buildCommand": "flutter build web --release || echo 'Using prebuilt build/web'",
  "outputDirectory": "build/web",
  "cleanUrls": true,
  "routes": [
    {
      "handle": "filesystem"
    },
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}
